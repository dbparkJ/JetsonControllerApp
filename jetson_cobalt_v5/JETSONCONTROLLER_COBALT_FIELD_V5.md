# Jetson Controller — Cobalt Field 2026
## Cobalt Field V5 — 통합 디자인 세트 및 AI-agent 실행 지시서

> 작성: 2026-09-08 · 대상: `dbparkJ/JetsonControllerApp`  
> 일회성 지시 파일: `JETSONCONTROLLER_COBALT_FIELD_V5.md`  
> 영구 규칙: `COBALT_FIELD_STYLE_GUIDE.md` · 시작 프롬프트: `START_PROMPT.txt`  
> 상태: **디자인·로컬 프로토타입·구현 지시서 제작. 네이티브 앱 구현 및 실기기 검증 결과물이 아님.**

---

## 0. 가장 먼저 읽을 범위와 증거 수준

이번 작업은 기존 앱의 색상만 바꾸는 작업이 아니다. **현재 작업의 중요도, 다음 행동, 장비 상태의 신뢰도를 먼저 보여주는 네이티브 화면 리빌딩**이다. 기존 UI 코드는 교체할 수 있지만 실제 기능은 검증된 데이터·명령 계층에 다시 연결한다.

이 세트는 V4의 초록·라임색 지침을 대체한다. **V5의 Cobalt Field 방향을 우선**하되 V3/V4에서 정리한 안전·상태·장비별 복원 계약은 보존한다. 이전 지시 파일과 `AGENTS.md`를 무단 삭제하지 않는다. Claude/Opera 접속 문제를 다시 해결하는 것은 선행 조건이 아니다.

| 구분 | 이번에 확보한 것 | 확보하지 못했거나 다음 구현에서 검증할 것 |
|---|---|---|
| 사용자 근거 | `reference/`의 원본 캡처 8장 | 원본 웹의 실제 클릭·네트워크 동작 |
| 새 디자인 | Cobalt 팔레트로 동기화한 4탭·토큰·정상/오류/모달 로컬 시안 17개 | Android 폰트·인셋·스크롤에서의 실제 렌더링 |
| 디자인 도구 | V4의 Figma 계정·Material 3 라이브러리 조사 이력; 이번에는 로컬 생성 코드 동기화 | 최종 화면의 Figma 실파일 생성·검수·PNG 내보내기 |
| 제공 PNG | Chromium에서 자체 HTML을 렌더링한 시안 | Figma export 또는 실제 앱 캡처가 아님 |
| Figma 플러그인 | 편집 가능한 레이어 생성 코드, 변수·스타일·컴포넌트 및 PNG export 기능 | 실제 Figma에서 실행한 검증은 미실행 |
| 테스트 | 로컬 프로토타입 검사, 대비 계산, 플러그인 mock 검사 | 네이티브 빌드·TalkBack·Jetson/스마트폰 검증 |

이전 V4 작업에서는 Figma MCP 호출 제한이 기록되어 있었다. 이번 V5 묶음 제작에서는 계정·한도·초안 파일 상태를 새로 조회하지 않았다. 제한을 회피하거나 성공한 것처럼 보고하지 않는다. `figma-plugin/`은 **로컬 개발 플러그인으로 실행할 별도 산출물**이며, 실행하지 않은 상태를 편집 완료로 세지 않는다.

**판단 우선순위:** 사용자 최신 요구 → 실제 장치/API의 사실과 안전 → 핵심 작업 흐름 → V5 디자인 의도 → 시안의 픽셀·예시 데이터 → 기존 UI 모양.

상충하는 부분은 스스로 근거를 확인해 조정한다. 단, 사용자 데이터 삭제·운영 장비 전원/네트워크 변경·외부 비용 발생을 디자인 판단에 포함하지 않는다.

---

### 0.1 이전 MD를 그대로 사용하지 않는 이유와 이번 세트의 기준

이전 V4는 포리스트·라임색을 명시한 문서다. 마지막에 제시한 **Cobalt Field** 이미지와 색상 지시가 다르므로, **이번 V5 한 세트를 사용한다. V4를 동시에 활성 지침으로 전달하지 않는다.** V4의 정보 구조·기능·상태·안전 계약은 이 문서 안에 통합했으므로 이전 MD를 별도로 읽어야 하는 의존성은 없다.

이번 V5는 다시 다른 콘셉트를 만든 것이 아니라 **마지막 코발트 시안과 문서·코드·PNG를 맞춘 통합 인계본**이다. 초록색 거부를 기능 변경 요구로 해석하지 않는다.

| 자료 | 이번 역할 |
|---|---|
| `design/tokens.json` | 명시적인 Light/Dark 색상·크기 기준 |
| `screens/00-overview.png`, 개별 PNG 17개 | Cobalt Field의 정보 위계·형태·배치·대표 상태 |
| `design/scene-spec.json`, `prototype.html` | 같은 토큰을 사용하는 편집 가능한 로컬 시안 소스·동작 참고 |
| `figma-plugin/` | 같은 팔레트로 편집 가능한 Figma 레이어를 생성하기 위한 코드; 실제 Figma 실행은 별도 검증 |
| `reference/01_user_reference.png`~`08_user_reference.png` | 처음 제공된 흐름·기능 관찰용 캡처. 남아 있는 청록색은 최신 팔레트가 아님 |
| `reference/last_shown_cobalt/` | 직전 답변의 Cobalt 홈·4화면 보드 원본. 이번 재생성의 대조 자료 |
| 이전 V1~V4 문서·생성형 홍보 보드 | 이번 구현의 활성 기준이 아님. 저장소에 이미 있으면 보존/이력 처리하되 서로 다른 색상·기능을 혼합하지 않음 |

**정확한 색은 tokens, 화면 의도는 이번 PNG, 행동·안전은 본문의 계약, 실제 가능 여부는 장치/API가 결정한다.** 이미지에 예시값이 보여도 실제 데이터가 없으면 정상값을 만들어 넣지 않는다. 원본 캡처와 생성형 홍보 이미지의 iOS 프레임·가짜 장치·임의 진행률·원격 명령 제안은 제품 요구로 확대하지 않는다.

V4가 이미 일부 코드에 구현되었다면 색상 토큰·표면 계층·상태색·공통 컴포넌트의 필요한 차이만 바꾼다. V3 이전 화면이 남았다면 4탭과 새 흐름을 단계적으로 구현한다. 다른 문서를 자동 삭제하거나 기능을 다시 작성하는 것으로 시작하지 않는다.

### 0.2 색상 이행과 범위

주요 작업 표면은 네이비, 실행/선택 강조는 코발트 또는 아이스 블루, 바탕은 따뜻한 오프화이트로 통일한다. **브랜드뿐 아니라 성공·완료 배지에도 초록색을 다시 넣지 않는다.** 성공·완료는 인디고 계열과 체크/문구, 진행은 파랑과 진행 문구, 주의는 앰버, 실패·위험은 코럴/레드로 구분한다. 표시는 색만으로 구분하지 않는다.

이번 묶음은 시안·가이드·프로토타입·생성 코드의 정합성 작업이다. 실제 앱 코드를 수정·커밋하거나 Figma 파일을 새로 완성했다고 보고하지 않는다. 네이티브 구현과 실기 검증은 이 지시서를 받는 에이전트의 후속 작업이다.

---

## 1. 원본 캡처에서 유지한 것과 새로 설계한 것

### 1.1 원본에서 확인한 것

S01~S08은 `reference/01_user_reference.png`~`08_user_reference.png`에 대응한다. 원본명과 SHA-256은 `reference/manifest.json`에 있다.

| 근거 | 관찰 |
|---|---|
| S01 / S02 | 장비 헤더, 현재 작업, 준비 상태, 전송 대기 요약, 홈 선택 |
| S03 | 실행 중·시작 요청 중·실패 작업 카드, 추가·상태·중지 동작 |
| S04 / S05 | 전송 대기/장비 파일, 대상·용량 확인, 선택, 이력, 하단 전송 버튼 |
| S06 / S07 | 장비 설정·앱 설정·위험 동작, 테마와 상태 보존 토글 |
| S08 | 장비명과 중단 영향을 안내하는 재부팅 확인창 |

8장은 밝은 테마, 기본 글꼴, 정상 연결, 등록된 장비를 선택한 정지 화면이다. 어두운 테마, 큰 글꼴 배치, 끊김 상태, 등록 전 화면, 상세 동작은 직접 관찰한 원본이 아니라 **이번에 보완한 설계**다.

왼쪽 테스트 패널, 검은 휴대전화 프레임, 가짜 시계·배터리, 웹 스크롤바는 제품 화면에 넣지 않는다. 시안의 “조건에 따라 문구가 바뀌지 않는다”는 설명은 제품 요구가 아니다. 제품에서는 실제 상태에 따라 문구·가용 행동이 바뀌어야 한다.

### 1.2 핵심 판단

**홈·작업·데이터·설정의 구조는 유지하고 시각 언어와 동작 흐름은 리빌딩한다.** 사용자는 새로운 메뉴를 다시 학습하기보다, 지금 실행 중인 작업과 문제가 무엇인지 더 빨리 읽을 수 있어야 한다.

| 항목 | V5 결정 | 이유 |
|---|---|---|
| 현재 작업 | 짙은 네이비 표면의 핵심 카드 | 다른 관리 항목보다 먼저 읽히게 함 |
| 화면 배경 | 약간 따뜻한 중립색 | 검은 글자·짙은 작업 카드와 명확한 위계 |
| 강조색 | 코발트·아이스 블루 포인트 | 주요 행동·선택에만 집중; 성공 의미와 분리 |
| 준비 상태 | 요약 타일 + 필요한 상세 | 정상 항목의 긴 반복 행을 줄임 |
| 카메라/GNSS | 홈의 명확한 바로가기 | 센서 탭을 없애도 자주 쓰는 기능이 멀어지지 않게 함 |
| 하단 내비게이션 | 불투명한 고정 영역 + 선택 capsule | 원본의 선택 강조는 유지, 떠 있는 바의 여분 공간·중첩을 줄임 |
| 데이터 선택 | 실제 전송 단위 중심 | 폴더 API에 파일 체크 UI를 억지로 붙이지 않음 |
| 상태 보존 토글 | 제거; 자동 보존 | 복원은 사용자가 켜야 하는 옵션이 아님 |
| 팬·전원·진단 | 설정 안으로 이동 | 능력은 유지하면서 홈의 기술적 잡음을 줄임 |
| 위험 확인 | 대상·영향·구체적 확정 행동 | 실수 방지, 모호한 “확인” 버튼 제거 |

`153 GB`, `42분`, `128개`, `RTK FIX`, 서버 이름, 경로와 시각은 모두 **시안용 데이터**다. 없는 API 필드를 제품 코드에 만들어 넣거나 release에서 기본 정상값으로 사용하지 않는다.

---

## 2. 2026 시점의 디자인 방향: 무엇을 채택할 것인가

여기서 말하는 “2026 방향”은 모든 제품이 따라야 하는 유행 목록이나 시장점유율 주장이 아니다. **현재 공개된 Google·Apple·Android 공식 지침에서 이 현장용 Android 앱에 도움이 되는 부분을 선택한 설계 판단**이다. 2025년에 소개된 방향도 2026 시점에 참고한 것으로 표시한다.

### 2.1 목적 있는 표현력

Google의 Material 3 Expressive 설명은 표현력, 사용성, 접근성과 상황 적합성을 함께 다루며, 표현적인 디자인이 반드시 요란해야 하는 것은 아니라고 설명한다. [D1]

**적용:** 현재 작업에만 강한 명암, 의미 있는 크기 차이, 주요 행동의 색 대비를 준다. 모든 카드를 같은 강조색으로 만들거나 모든 상태를 움직이게 만들지 않는다. 네이비/코발트 조합 자체가 공식 M3 팔레트라는 뜻은 아니다. 이는 이번 제품을 위한 신규 제안이다.

### 2.2 콘텐츠와 제어 영역의 분리

Apple의 Liquid Glass 설명은 콘텐츠와 내비게이션/제어 레이어의 역할을 구분하며 유리 효과의 무분별한 중첩을 피하도록 안내한다. 이 자료는 Apple 플랫폼의 방향이다. [D2]

**적용:** 하단 탐색과 상황별 주요 행동을 하나의 측정 가능한 영역으로 분리한다. **Android 현장 도구에는 유리 굴절·배경 블러를 이식하지 않는다.** 이 원칙은 시각적 구분을 위한 참고이지 iOS 컴포넌트 복제 지시가 아니다.

### 2.3 접근성을 예외가 아닌 기본 구성으로

Compose 기본 컴포넌트는 역할별 semantics와 터치 영역을 제공하며, 공식 안내는 상호작용 대상의 최소 크기 48dp를 설명한다. [D3] Android는 200%까지의 비선형 글꼴 확대를 지원하므로 실제 OS 설정에서 검증해야 한다. [D4]

**적용:** 주요 버튼은 최소 52dp, 주요 행은 48dp 이상, 본문은 16sp를 시작값으로 사용한다. 고정 높이에 글자를 끼워 맞추지 않는다. 로컬 HTML의 2.0배는 **선형 스트레스 시안**일 뿐 Android의 비선형 확대를 재현한 것이 아니다.

### 2.4 이 제품에 추가로 적용하는 판단

“연결됨”보다 **현재 어떤 동작이 가능한지**, “실행 중”보다 **언제 확인한 상태인지**가 중요하다. 따라서 최신성, 확인 불가, pending 명령, 미지원 이유를 주요 UI 정보로 다룬다. 이는 이번 앱의 성격에서 도출한 제품 판단이며 통계적으로 검증된 유행이라고 주장하지 않는다.

AI 챗봇, 자동 행동 추천, 3D 장비 모델, 화려한 지도·차트를 트렌드라는 이유로 넣지 않는다. 현재 앱에서는 잘못된 상태나 중복 명령을 줄이는 것이 우선이다.

---

## 3. Cobalt Field 스타일 가이드

### 3.1 디자인 원칙

**명확한 위계 / 상태의 정직함 / 적은 장식 / 일관된 행동 / 장비 문맥 유지**를 공통 기준으로 삼는다.

루트 화면 하나에는 가장 중요한 카드와 행동이 분명해야 한다. 정상 데이터를 장식 그래프로 바꾸지 않는다. 카드 크기는 정보량으로 정하며, 큰 여백으로 클릭 수가 늘어나면 조정한다. 실패나 끊김을 숨기지 않되 화면 전체를 빨갛게 만들지 않는다.

### 3.2 색상 토큰

`design/tokens.json`이 정확한 값의 기준이다. 다음은 **마지막 Cobalt 시안에 맞춘 V5 역할 기반 팔레트**다. Light의 핵심 표면·문자·주요 버튼 역할은 직전 PNG의 평탄한 픽셀 영역을 대조했다. 세부 상태색 쌍과 Dark는 누락된 역할을 완성한 제안값이다. 보이지 않은 원본 화면에서 추출했다고 주장하지 않는다.

| 역할 | Light | Dark |
|---|---|---|
| canvas | `#F7F5F2` | `#101827` |
| surface | `#FFFFFF` | `#182338` |
| subtle | `#EDF1F7` | `#25324B` |
| ink | `#1D293B` | `#EDF2FC` |
| muted | `#5D687A` | `#B8C4D9` |
| border | `#D9DFE8` | `#3D4E69` |
| primary / onPrimary | `#2456D8` / `#FFFFFF` | `#9CBBFF` / `#10265A` |
| accent / onAccent | `#DCE7FF` / `#173B82` | `#DCE7FF` / `#173B82` |
| hero / heroText | `#172B4D` / `#F6F8FF` | `#203553` / `#F6F8FF` |
| heroMuted | `#C3CEE2` | `#C3CEE2` |
| success / successBg | `#384B92` / `#EEF0FF` | `#C2CBFF` / `#303954` |
| warning / warningBg | `#8A4E19` / `#FFF0DD` | `#FFD39A` / `#473620` |
| danger / dangerBg | `#AE382C` / `#FCECE8` | `#FFB8AC` / `#4A2B2A` |
| info / infoBg | `#315E94` / `#EAF2FC` | `#BBD3FF` / `#243958` |
| disabled / onDisabled | `#E5E9EF` / `#657083` | `#2E3A51` / `#B0BCD0` |

브랜드 포인트와 상태 의미를 분리한다. 코발트는 주요 행동, 아이스 블루는 선택/짙은 표면 위 행동, 인디고는 확인된 성공·완료 역할이다. 파란색이라는 이유만으로 “성공 완료”를 판정하지 않는다. 초록·청록·포리스트·라임을 브랜드와 상태 배지에 다시 넣지 않는다. 배지에는 반드시 `실행 중`, `시작 요청 중`, `실패`, `현재 상태 미확인`처럼 문구를 넣는다.

본문과 보조 설명의 사용 쌍은 대비 4.5:1 이상을 프로젝트 목표로 한다. 의미를 전달하는 경계/아이콘은 3:1 이상을 별도 검사한다. 장식용 연한 카드 테두리만으로 선택 상태를 전달하지 않는다. 제공된 대비 검사는 24개 대표 텍스트 쌍의 계산이며 앱 전체 접근성 인증이 아니다.

MaterialTheme의 `surfaceContainer*`, `onSurfaceVariant`, container/on-container 쌍, error 계열까지 명시해 기본 보라색·청록색 등 의도하지 않은 팔레트가 일부 화면에 섞이지 않게 한다. 인디고 상태색은 명시된 토큰만 사용한다. 색을 화면별로 하드코딩하지 않는다. 시스템 따름·밝게·어둡게를 제공하고 기존 설정을 유지한다.

### 3.3 타이포그래피

| 역할 | 시작 크기 / 굵기 | 사용 |
|---|---|---|
| 화면 제목 | 26sp / 700 | 작업, 데이터, 설정 |
| 핵심 카드 제목 | 24sp / 700 | 현재 작업 |
| 작업/그룹 제목 | 20sp / 700 | 목록 카드, 요약 |
| 중요한 수치 | 23~28sp / 700 | 실제 값이 있을 때만 |
| 일반 본문/행 제목 | 16sp / 400~500 | 주요 정보 |
| 보조 설명 | 14sp / 400 | 원인, 다음 행동 |
| 버튼 | 15~16sp / 700 | 실행/조회 |
| 부가 메타/배지/탭 | 12~13sp / 500~700 | 단위, 보조 시각, 상태 |

Figma용 폰트는 Noto Sans KR을 우선한다. 로컬 PNG는 환경의 Noto Sans CJK KR로 렌더링했다. Android에서는 기존 `FontFamily.SansSerif` 또는 검증된 프로젝트 폰트를 우선하며 시안과 픽셀 일치를 위해 새 대용량 폰트를 강제로 번들하지 않는다. **폰트 바이너리는 패키지에 포함하지 않는다.**

한글 본문 줄높이는 약 1.5, 큰 제목은 약 1.32를 출발값으로 한다. 실제 한글 잘림·기기 폴백을 확인한다. 오류 이유를 12sp의 한 줄로 줄여 숨기지 않는다. 숫자 소수점, 파일 확장자, device ID의 판별 가능한 부분을 보존한다.

### 3.4 간격·형태·아이콘

화면 좌우 여백은 20dp를 기본으로 하고 360dp 이하에서는 16dp까지 조정할 수 있다. 카드 내부는 16~20dp, 큰 설명 카드/모달은 24dp를 사용한다. 간격의 기본 단위는 4dp이며 8/12/16/20/24/32dp를 중심으로 정리한다. 로컬 시안에 사용된 작은 보정 간격을 모든 화면에 새 토큰으로 증식시키지 않는다.

배지 8dp, 작은 제어 12~14dp, 일반 그룹 18~20dp, 핵심 카드 24dp, 모달 26~28dp 안팎을 출발값으로 쓴다. 원형은 작은 아이콘 버튼과 선택 표시처럼 의도가 있을 때만 사용한다.

아이콘은 하나의 선형 계열을 사용한다. 제공 SVG는 24 좌표계의 자체 제작 참고 아이콘이며 PNG가 아니다. 실제 앱에서는 기존 Material 아이콘으로 같은 의미를 유지해도 된다. 아이콘만 있는 버튼은 48×48dp와 접근성 이름을 제공한다.

### 3.5 모션·성능

전환의 출발값은 단순 상태 강조 120~180ms, 상세/확인창 180~240ms다. 이는 제품 제안값이며 실측 결과가 아니다. 이동·확대·흔들림이 핵심 정보를 방해하면 생략한다. OS 모션 설정을 존중하고, 변화가 없는 숫자·아이콘을 계속 애니메이션하지 않는다.

배경 블러, 굴절, 무한 shimmer, 3D, 홈 자동 영상, 새 지도 SDK, 대형 차트 라이브러리를 추가하지 않는다. 새 디자인이 별도의 폴링 루프나 상시 카메라 연결을 만들면 실패다. 예시 PNG·원본 캡처·HTML은 release APK에 넣지 않는다.

### 3.6 공통 구성요소 계약

`DeviceContextHeader`, `ConnectionNotice`, `CurrentTaskCard`, `ReadinessTile`, `TaskStateCard`, `FolderSelectionRow`, `TransferActionArea`, `GroupedSettings`, `ConfirmDeviceAction`, `EmptyState`, `CobaltNavigation`을 화면 간 공유한다. 이미 같은 책임의 구성요소가 있으면 중복 작성하지 않고 정리한다.

이번 로컬 Figma 생성기는 **Button/Badge variants, 아이콘 인스턴스, 텍스트 스타일과 색상·간격 변수**를 생성하도록 작성했다. 모든 고수준 카드가 이미 완전한 재사용 컴포넌트로 출판됐다고 가정하지 않는다. 구현 단계에서는 카드와 shell까지 프로젝트 규칙에 맞게 공통화한다.

---

## 4. 정보 구조와 화면별 구현 계약

### 4.1 내비게이션

```text
홈        : 선택 장비 / 현재 작업 / 준비 상태 / 결과 요약
작업      : 목록 / 추가 / 시작 전 확인 / 상세 / 설정·로그·결과
데이터    : 전송 대기 / 장비 파일 / 대상·선택 확인 / 진행·검증·이력
설정      : 네트워크·센서·팬·서버·진단 / 앱 설정 / 위험 동작
공통      : 장비 선택·등록 / 알림 목록 / 연결 복구
```

센서와 알림을 별도 하단 탭에서 제외하는 것은 **접근 경로 통합**이다. 카메라·GNSS·RTK·IMU·알림 이력·임계값 설정 기능을 삭제하지 않는다. 모든 장비 관련 화면에서 선택 장비를 식별할 수 있어야 한다. 루트 헤더의 알림 진입점은 공통으로 유지한다.

하단 탭을 다시 누를 때 중복 route를 쌓지 않는다. 상세에서 돌아오면 이전 탭·스크롤·필터로 복귀한다. 전송 CTA와 내비게이션은 공통 하단 영역에서 함께 측정해 본문이 둘의 전체 높이를 피하도록 한다. IME와 Android 시스템 인셋을 두 번 더하지 않는다.

### 4.2 홈

**현재 작업 → 준비 상태 → 결과** 순서를 유지한다. 짙은 카드에는 작업명, 실제 실행/명령 상태, 출처가 있는 경과 시간과 저장 결과만 표시한다. 상태 보기와 중지는 분리한다. 중지는 접근 가능하게 두되 실수로 누르기 쉬운 주 행동과 동일하게 강조하지 않는다.

필수 조건을 모두 확인하지 못했는데 “수집 준비 완료”라고 쓰지 않는다. 저장률만 있으면 남은 GB를 계산해 만들어 넣지 않는다. 여러 작업이 실행 중이면 대표 1개와 `실행 중 N개`로 목록에 연결한다.

연결이 끊긴 뒤에는 `작업 없음`으로 바꾸지 않는다. 마지막 실행 상태와 확인 시각을 보관하고 `현재 상태 미확인`으로 구분한다. 실제 카메라 프레임 없이 홈 썸네일에 LIVE를 붙이지 않는다.

### 4.3 작업 목록·상세·추가

작업 카드의 기본 정보는 작업명, 실행 상태, 최근 결과, 다음 행동이다. Python 경로·부팅 시 실행·등록 해제·로그 전문은 상세/메뉴로 이동한다. 기능 자체를 제거하지 않는다.

| 상태 | 표시와 행동 |
|---|---|
| 대기 | 실제 조건이 맞으면 시작; 시작 전 확인으로 연결 |
| 시작 요청 중 | 요청 접수/전송 중 표시; 이중 요청 차단 |
| 실행 확인 | 실행 중; 상태·결과·중지 접근 |
| 실패 | 짧은 원인과 문제 확인; 조건을 충족할 때만 재시도 |
| 중지 요청 중 | 요청과 실제 종료를 분리; 완료를 앞당겨 표시하지 않음 |
| 응답 확인 불가 | 이전 상태+확인 필요; 재명령보다 조회 우선 |
| 미지원/권한 부족 | 원인과 해결 경로; 가짜 활성 버튼 금지 |

신규 **시작 전 확인**은 기존 capability·작업 요구사항을 재사용한다. 작업을 시작해야 활성화되는 카메라 때문에 영원히 시작할 수 없는 순환 조건을 만들지 않는다. 필수 조건은 차단, 선택 조건은 안내로 분리한다. 단순한 일상 실행마다 불필요한 다중 모달을 추가하지 않는다.

상세는 상태·결과·로그·설정의 역할을 구분한다. UI를 열거나 회전했다는 이유로 작업을 시작하거나 부팅 설정을 다시 저장하지 않는다. 로그는 필요할 때 읽고 계속 전체 목록에 방송하지 않는다.

### 4.4 데이터·전송

원본의 “파일 3개 선택” 모양을 그대로 강제하지 않는다. 조사한 기존 업로드 흐름의 입력은 `rootId`, `path`, `targetId` 기반이다. 임의 파일 묶음 API가 이미 있다고 단정할 수 없다. [R1]

실행 에이전트는 현재 `JetsonRepository.startUpload`와 백엔드 검증을 끝까지 읽어 지원 단위를 확정한다. 폴더 단위면 V5처럼 `폴더 선택/폴더 N개 전송`으로 구현한다. 단일 폴더 job만 지원하면 여러 폴더 선택을 하나의 job이라고 꾸미지 않는다. 검증된 순차 배치와 개별 job 대응을 만들거나 우선 한 폴더씩 전송하게 조정한다.

파일 단위가 실제 지원될 때만 파일 체크 UI를 제공한다. **3개 선택 후 부모 폴더 전체를 전송하는 행동은 금지**한다. 루트·정규화 경로·권한·출처 요약 검사를 그대로 유지한다.

새 항목은 기본 미선택이다. 시안의 1개 체크는 사용자가 선택한 후의 예시다. 선택을 해제하는 것은 원본 삭제가 아니다. 검색/필터로 가려진 선택은 개수와 범위로 드러내고 장비 A의 선택을 B에 넘기지 않는다.

전송 준비는 제어 연결, 전송 경로, 서버 설정, 서버 인증/접근, 원본 선택, 용량 확인을 분리한다. 과거 확인한 `TransportPolicy.kt`는 LAN에서만 서버 업로드 시작을 허용한다. UI 때문에 이 정책을 해제하지 않는다. 모바일 RTK 중계는 이 파일 전송 기능과 별개다. 현재 브랜치에서 정책이 달라졌다면 코드·테스트·실제 경로를 함께 확인한다. [R2]

LAN 연결만으로 `서버 응답 확인됨`을 표시하지 않는다. 사전 확인 API가 없으면 `대상 서버 접근은 전송 시 확인`처럼 사실대로 쓴다. 크기 미확인을 0 B로 표시하지 않고 기존 필수 검사를 우회하지 않는다.

전송 직전에 **대상 장비 → 대상 서버 → 선택 단위/개수/알려진 용량**을 확인한다. 대기·전송·검증·완료·실패를 분리하고 실제 바이트/서버 값이 없으면 가짜 진행률·남은 시간을 만들지 않는다. 실패한 job/원본만 안전하게 재시도하며 완료 항목 전체를 재전송하지 않는다. 일시정지/재개는 실제 API가 있을 때만 노출한다.

### 4.5 설정·위험 동작

설정은 실제 허브로 만든다. 기존 알림 설정 화면의 제목만 `설정`으로 바꾸지 않는다. 네트워크, 센서, 팬, 서버 대상, 장비 진단과 앱 알림·테마에 연결한다.

`장비 전환 시 화면 상태 유지` 토글은 제거한다. 장비별 스크롤·검색·선택·초안 보존은 기본 동작이다. 저장된 정상 상태를 현재 live 상태로 복원하는 기능은 아니다.

팬 지원 여부, 자동 모드, 최소 수동 속도는 실제 장비가 결정한다. 팬이 없는 장비는 해당 항목을 숨기거나 명확하게 미지원으로 표시한다. Wi-Fi Direct 전환의 공유기·휴대전화 인터넷·서버 전송 영향은 현재 정책에 맞게 알린다.

재부팅·전원 종료·등록 삭제는 각기 다른 기능이다. 확인창에 대상 장비와 실제 중단 영향을 넣고 구체적인 확정 버튼을 사용한다. 취소·뒤로 가기·바깥 닫기는 명령 0회다. 모달 중 장비가 바뀌면 닫거나 재확인한다. `1~2분` 등 재부팅 시간을 측정하지 않았다면 표시하지 않는다.

등록 삭제가 측정 파일 삭제나 장비 초기화라는 뜻이 되면 안 된다. 코드상 부수 효과를 확인해 안내한다. 경고를 닫았다는 사실을 문제 해결로 바꾸지 않는다.

### 4.6 센서·영상·GNSS

구성 여부, 장치 연결, 수신, 데이터 최신성은 별도 사실이다. 홈과 상세가 같은 상태 원천을 사용하도록 한다. 카메라 바로가기는 실제 프레임 스트림을 필요할 때 열며, 화면 이탈 시 미리보기 자원을 정리한다. 앱 미리보기를 닫았다고 Jetson의 수집 작업을 중지하지 않는다.

LIVE/지연/마지막 수신 화면을 구분한다. GNSS에서는 장비 위치와 휴대전화 위치, NTRIP 연결·RTCM 수신과 RTK FIX, 지도 로딩과 텔레메트리 수신을 분리한다. 실제 정확도·보정 나이가 없으면 값을 발명하지 않는다.

### 4.7 최초 등록·장비 전환·복구

미등록 화면에는 가짜 JETSON-01 연결됨 상태를 표시하지 않는다. QR, 수동 등록 코드, 권한 거절 대안을 유지한다. 등록과 인증, 연결과 제어 가능 여부를 구분한다.

장비 선택기는 현재 선택·발견됨·등록만 됨·미연결을 구분한다. A→B→A 전환은 A의 초안을 돌려주되 A의 늦은 응답이 B 화면에 들어오면 안 된다. 전환 자체가 작업 시작/중지 명령이 되어서는 안 된다.

끊김은 한 곳에 지속 배너로 보여주고 현재 화면과 입력을 유지한다. 연결 허브로 강제로 쫓아내지 않는다. 즉시 모달·Snackbar·카드 경고를 모두 중복 띄우지 않는다. 복구 화면은 경로별로 다음 행동을 안내하며 기존 재연결 엔진을 재사용한다.

---

## 5. 새 시안 목록과 비교 기준

`00-overview.png`는 주요 4개 화면의 소개 보드이며 `00-all-states.png`는 전체 상태 모음이다. 두 이미지는 개별 앱 화면 수에 포함하지 않는다.

| PNG / 프레임 키 | 목적 | 출처 |
|---|---|---|
| `01-home-light` | 현재 작업·준비 상태·결과 | S01/S02를 바탕으로 재설계 |
| `02-tasks` | 실행·요청·실패 목록 | S03를 바탕으로 재설계 |
| `03-data` | 폴더 선택과 전송 목적지 | S04/S05 + 실제 전송 단위 고려 |
| `04-settings` | 장비 설정 허브 | S06 재설계 |
| `05-home-dark` | 어두운 테마 | 신규 보완 |
| `06-home-offline` | 마지막 상태와 복구 | 신규 보완 |
| `07-task-detail` | 실행 상태·최근 기록 | 신규 보완 |
| `08-sensors` | 센서·영상 진입 | 신규 보완 |
| `09-preflight` | 시작 전 필수 조건 | 신규 보완 |
| `10-onboarding` | 미등록 상태·QR/수동 대안 | 신규 보완 |
| `11-connection` | 연결 경로별 복구 | 신규 보완 |
| `12-home-large-type` | 선형 2배 글꼴 스트레스 | 신규 보완; Android 비선형 확대와 다름 |
| `13-reboot-confirm` | 대상·영향·확정 | S08 재설계 |
| `14-transfer-confirm` | 선택/목적지 확인 | 신규 보완 |
| `15-settings-danger` | 하단 위험 동작 | S07 재설계 |
| `16-data-files` | 장비 파일 탭의 초기 방향 | 보완 시안; 실제 탐색은 재연결 필요 |
| `17-device-picker` | 등록 장비 선택 | 신규 보완 |

이 시안은 UI 방향과 대표 상태를 보여준다. `16-data-files`는 폴더 중심의 초기 표현이며 완성된 파일 탐색기를 뜻하지 않는다. 검색, 알림, 카메라 스트림, 서버 편집, 두 번째 장비 전환은 프로토타입에서 일부 안내/대표 화면까지만 동작한다. **누락 기능을 의도적 제거로 오인하지 말고 §4의 기능 계약에 따라 구현한다.**

---

## 6. 기능 추가·이동·제거의 범위

### 추가/강화 — 이번 필수 범위

시작 전 capability 확인, 요청 결과 확인 필요 상태, 전송 목적지·실제 선택 단위 확인, 일관된 장비 문맥, 장비별 자동 상태 보존, 연결 복구 안내, 큰 글꼴·어두운 테마, 센서/프레임 최신성 표시를 우선한다. 가능한 것은 기존 데이터·API로 구현하고 표현 개선 때문에 새로운 서버 서비스를 만들지 않는다.

### 통합/이동 — 능력은 유지

센서는 홈 바로가기와 설정, 알림은 공통 헤더, 로그·부팅 시 실행·작업 등록 해제는 작업 상세, 팬·전원·시스템 지표는 설정/진단으로 이동한다. 서버 보관함, RTK 관련 기존 기능, QR 수동 대안도 새 동선에서 접근 가능해야 한다.

### 제거 — UI 복잡성만 줄임

상태 보존 토글, 중복 연결 배너, 기술 설정이 먼저 보이는 홈 구역, 같은 목적의 여러 버튼, 불필요한 중첩 카드, release의 디버그 패널을 제거한다. backend 능력·인증·사용자 데이터·기존 안정화 로직을 제거하지 않는다.

### 보류 — 이번 필수로 만들지 않음

자동 AI 챗봇/명령 추천, 상시 지도와 영상 홈, 새 분석 차트, 다중 장비 동시 실시간 대시보드, 아직 없는 파일 단위 전송 API, 원격 장비 전원 켜기, 오프라인 지도 구축은 별도 기능 검토 대상이다. 문구만 먼저 만들어 정상 버튼으로 노출하지 않는다.

---

## 7. 상태·명령·복원 설계 계약

### 7.1 단일 truth와 표시 모델

기존 Repository/상태 엔진이 사실의 원천이다. 새 Screen마다 연결 상태 머신·watchdog·재시도 루프를 만들지 않는다. 최소한 다음 사실은 분리한다.

```text
선택 장비 / 등록·인증 / transport / 기능 capability
원격 실행 상태 / UI 명령 pending / 요청 결과의 확인 여부
마지막 telemetry 응답 / 센서 데이터 시각 / 카메라 프레임 시각
원본 선택 단위 / 전송 job / 서버 접근·검증 / 용량 가용성
```

`isOnline` 하나로 모든 기능을 정상 처리하지 않는다. display mapper는 가능한 한 순수 함수로 만들고 오래된 데이터와 미지원 사유를 테스트한다. 타임아웃·freshness 임계값은 기존 폴링 주기·정책·관찰값을 확인해 사용한다.

### 7.2 명령 흐름

`사용자 행동 → 대상/capability 재확인 → 필요한 영향 확인 → 1회 요청 → 접수 상태 → 실제 장비 상태 조회 → 성공/실패/확인 필요` 순서를 따른다.

HTTP 200이나 성공 콜백만으로 실행/재부팅 완료라고 표시하지 않는다. 재구성·Activity 재생성·탭 이동·자동 복구로 명령을 재전송하지 않는다. A장비의 늦은 응답은 A장비의 요청 세대로만 반영한다. 연결 세대 가드·백오프·중복 방지 기능을 중복 구현하거나 없애지 않는다.

### 7.3 복원

키는 `deviceId + screen/task identity`로 분리한다. 스크롤/선택/초안은 복원하되 live 상태는 재조회한다. 작은 UI 상태와 장기간 보존이 필요한 초안을 구분하고 대용량 이미지·로그·파일 목록을 SavedState Bundle에 넣지 않는다.

프로세스 재생성 후 pending 명령은 먼저 원격 상태와 조정한다. 앱 프로세스가 없어져도 장비 수집이 계속될 수 있음을 안내한다. 테마를 바꾸는 동작이 네트워크를 다시 설정하면 안 된다.

---

## 8. 저장소 적용 지도와 작업 순서

### 8.1 게이트 0

이 MD의 실제 경로를 기록한다. `AGENTS.md`, 브랜치·HEAD·미커밋 변경·최근 커밋, Gradle wrapper/카탈로그/CI, 기존 테스트, 연결 안정성 변경을 먼저 확인한다.

```bash
git status --short
git branch --show-current
git rev-parse HEAD
git log -5 --oneline
```

기존 분석의 baseline은 `d9764e756ae64161acd12c9e7bf93ca18ea98909`다. **현재 원격/로컬 HEAD가 같다고 가정하지 말고 이 SHA로 리셋하지 않는다.** `reset --hard`, `clean -fd`, 강제 push, 사용자 작업의 임의 stash를 사용하지 않는다. 원격 push/병합/배포는 별도 승인 범위일 때만 한다.

스마트폰·Jetson 접근이 있으면 대상 ID·OS·화면·연결 경로·서비스·실제 capability를 먼저 기록한다. 접근이 없다고 시각 구현과 독립 테스트를 전부 멈추지 않는다.

### 8.2 파일 지도

아래는 기존 조사에서 확인한 시작점이며 실행 시 현재 HEAD에서 다시 찾는다. 새로운 Screen/mapper 이름은 기존 프로젝트 관례를 따른다.

| 기존 파일/영역 | 새 책임 |
|---|---|
| `ui/JetsonApp.kt` | 공통 shell·4탭·장비 문맥·상세/알림 route |
| `ui/components/ControlNavigationBar.kt` | 홈/작업/데이터/설정으로 교체 |
| `ui/theme/*`, `ui/components/*` | Cobalt Field tokens, 상태·행동 컴포넌트 |
| `ui/dashboard/*` | 작업 우선 홈; 팬/전원/진단은 설정으로 |
| `ui/pipelines/*` | 작업 목록·상세·시작 전 확인·설정·로그 |
| `ui/storage/*`, `ui/upload/*` | 폴더/파일 실제 단위, 전송 확인·진행·이력 |
| `ui/connection/*`, `ui/devices/*`, `ui/pairing/*` | 장비 선택·등록·복구 |
| `ui/sensors/*` | 영상·GNSS·IMU 상세와 최신성 |
| `ui/settings/*`, `ui/alerts/*` | 설정 허브와 알림 이력/설정 분리 |
| `data/transport/*`, `JetsonRepository` | 기존 연결·권한·가용성 정책 유지 |
| `app/src/test`, `app/src/androidTest`, CI | 화면 계약·기능 회귀·상태 테스트 |

`Screen`은 UiState와 콜백을 받고 Route/ViewModel에서 기존 데이터에 연결한다. `JetsonApp.kt`에 모든 화면/정책을 더하지 않는다. 유행하는 아키텍처라는 이유로 framework·DI·상태관리·Navigation 전체를 갈아엎지 않는다.

### 8.3 단계별 구현

| 단계 | 수행 | 검증/산출 |
|---|---|---|
| 0 | 기준 코드·변경·capability·원본과 새 시안 확인 | 기능 지도, 수정 경계 |
| 1 | 토큰·공통 컴포넌트·shell·4탭·설정 허브 | 실제 Compose 렌더링 |
| 2 | 홈/작업/데이터의 시각·상태 구현 | debug 갤러리, 밝게/어둡게/큰 글꼴 |
| 3 | 기존 기능 재연결 | 장비→작업→전송→센서→설정/알림 |
| 4 | 복원·끊김·late response·명령 중복 검증 | 순수 상태 및 UI 테스트 |
| 5 | 승인된 실기기 검사·성능 비교 | 실제 캡처·로그·결과 |
| 6 | 구형 UI 정리와 최종 인계 | 최종 가이드·보고서·완료 판정 |

각 단계에서 발견한 문서와 장치의 차이는 `관찰 → 채택한 변경 → 사용자 영향 → 검증` 형식으로 기록한다. 불필요한 디자인 선택 확인은 반복 요청하지 않는다. 데이터 삭제나 운영 영향이 있는 행동은 해당 승인 절차를 지킨다.

---

## 9. Figma 활용과 로컬 플러그인 인계

### 9.1 가능한 경로

Figma MCP가 사용할 수 있으면 실제 파일·선택 노드·변수·라이브러리를 읽고 작업한다. 파일/노드 ID를 추정하지 않는다. 이전 V4 작업에서 알려진 초기 초안 키는 `DhyCybqq65CPBwomkElI9r`이지만 **이번 V5 작업에서 재조회하거나 수정하지 않았다. 이 파일이 완성 화면이나 코발트 팔레트를 포함한다고 가정해서는 안 된다**.

MCP가 계속 제한되면 `figma-plugin/README.md`를 따른다. 이 패키지의 개발 플러그인은 로컬에서 실행해 현재 페이지에 별도 스크린 보드와 컴포넌트 보드를 추가하도록 작성했다. 기존 레이어를 지우지 않는다. Figma가 발급한 plugin ID를 사용하며 API 키·네트워크 접근을 요구하지 않는다. [F1][F2]

### 9.2 생성 설계

- 실제 텍스트와 SVG 벡터, Auto Layout 프레임으로 구성한다. 화면 PNG를 붙이는 방식이 아니다.
- Light/Dark는 별도의 단일 모드 semantic 컬렉션으로 만들어 다중 모드 사용 권한에 의존하지 않는다. primitive alias와 Android code syntax를 사용한다.
- Button/Badge의 theme·tone·text scale variant, 편집 가능한 label, 아이콘 인스턴스, 텍스트 스타일을 구성한다.
- 대표 탭/상세/확인 화면의 prototype 이동을 연결한다. 계측·장비 명령 실행은 하지 않는다.
- 선택 화면의 PNG 2배 export 요청을 처리하는 UI를 제공한다. export가 실제 성공한 뒤에만 해당 파일을 Figma PNG라고 부른다. [F3]

현재 mock 검사 결과는 17개 화면, 18개 사용된 Button/Badge variant, 151개 변수, 38개 텍스트 스타일의 **생성 경로**를 확인한 것이다. 이 숫자는 실제 Figma에서 검수·출판된 라이브러리 수량이 아니다. 실제 Figma의 폰트·auto layout·API 지원은 실행 후 확인한다.

### 9.3 실행 후 필수 검수

글꼴 로드, 텍스트 줄바꿈, 자동 높이, 버튼 label 수정, 밝게/어둡게 역할, 긴 파일명, 스크롤 내용의 하단 접근, 모달 범위, 탭 목적지를 확인한다. 제공 로컬 PNG와 의도적 차이를 기록한다.

잘린 텍스트를 PNG로 대체하지 않는다. 시안의 정확한 크기는 412×892 논리 픽셀이지만 Android의 dp/sp 또는 실물 화면 크기와 동일하다고 가정하지 않는다. 캔버스 제작용 폰트/일부 아이콘/OS 영역은 플랫폼에 맞게 바뀔 수 있다.

Code Connect는 현재 Kotlin 컴포넌트가 실제로 구현되고 이름·props가 확정된 뒤 연결한다. 아직 없는 컴포넌트 파일 경로나 code mapping을 완료로 등록하지 않는다.

### 9.4 플러그인 실패 시

MCP 실패와 로컬 플러그인 실패는 별개로 기록한다. 폰트/manifest/권한 오류는 해당 원인만 해결하고 mock 통과를 근거로 무조건 정상이라고 주장하지 않는다. 부분 생성 시 그 보드를 `PARTIAL`로 표시하며 기존 레이어를 일괄 삭제하지 않는다. 안전한 되돌리기 후 재시도한다.

Figma에서 막힌 상태에서도 가이드·scene-spec·PNG·native Compose 구현은 독립적으로 진행한다. 다만 Figma 기능의 미검증 상태는 최종 보고에서 숨기지 않는다.

---

## 10. 검증 기준

### 10.1 이번 패키지의 검사 범위

`evidence/prototype-validation.json`: 17개 시안 × (412px/1.0, 360px/1.0, 360px/선형2.0) 총 51개 조합의 텍스트 가로 넘침 검사, 대표 이동/모달/선택 10개 상호작용 검사, Light/Dark 대표 색상 쌍 24개 대비 계산이다. 이는 가로 경계 검사 중심이며 **모든 동작·전체 접근성·실기 성능 통과를 뜻하지 않는다**.

`evidence/plugin-mock-test.json`: 코드 구문, 생성 제어 흐름, 중복 생성 가드, PNG export 메시지 전달을 가짜 API로 검사했다. 실제 Figma의 렌더러·네트워크·PNG export는 호출하지 않았다.

### 10.2 네이티브 자동 검사

현재 프로젝트 도구 체인에 맞춘다. 디자인 변경 때문에 SDK/Kotlin/Compose/Gradle을 일괄 업데이트하거나 기존 lint/회귀 검사를 끄지 않는다.

```bash
./gradlew :app:assembleDebug :app:lintDebug :app:testDebugUnitTest --console=plain
# 승인된 테스트 기기/에뮬레이터 준비 및 대상 확인 후
./gradlew :app:connectedDebugAndroidTest --console=plain
```

Windows에서는 해당 wrapper의 `.bat`를 사용한다. 기기가 여럿이면 대상을 명확히 고른다. 테스트 수·실패·skip·종료 코드와 보고서 경로를 기록한다. 실행 0개 또는 SDK 오류를 통과로 쓰지 않는다. 백엔드 수정이 있으면 해당 API와 기존 backend 회귀도 실행한다.

### 10.3 필수 수용 테스트

| ID | 검사 | 통과 조건 |
|---|---|---|
| A01 | 4탭·뒤로·상세 | 순서·선택·복귀 위치 일치, 중복 stack 없음 |
| A02 | 루트 장비 문맥 | 잘못된 장비에 명령을 보낼 여지가 없음 |
| A03 | 장비 A→B→A | 선택·초안 분리; 늦은 A 응답이 B에 반영되지 않음 |
| A04 | 시작 pending | 클릭 1회→요청 1회; 실제 실행 확인 전 running 금지 |
| A05 | 응답 중 연결 손실 | 확인 필요 표시; 명령 자동 재전송 없음 |
| A06 | 재구성·회전·프로세스 복원 | 명령 재실행 없음; UI 상태는 올바르게 복원 |
| A07 | 시작 전 센서 조건 | 기동 후 활성 센서를 영구 차단 조건으로 만들지 않음 |
| A08 | 미확인 상태 | 센서·저장 공간·서버 확인을 정상값으로 대체하지 않음 |
| A09 | LAN/Direct/BLE | 실제 capability와 업로드 정책 일치 |
| A10 | 파일/폴더 선택 | 선택한 실제 단위만 전송, 경로 검증·출처 요약 유지 |
| A11 | 새 항목·필터·장비 변경 | 새 항목 자동 선택 없음, 선택 범위 명확 |
| A12 | 전송 목적지 | 서버/장비/단위/크기 일치; 용량 미확인 명시 |
| A13 | 진행·검증·재시도 | 가짜 진행률 없음, 완료 job 중복 재전송 방지 |
| A14 | 위험 확인 취소 | 뒤로/취소 시 명령 0회, 확정 시 1회 |
| A15 | 모달 중 장비 전환 | 대상 재확인 또는 안전한 모달 종료 |
| A16 | 끊김/알림 | 중복 배너 없음, dismiss와 장애 해소 구분 |
| A17 | 카메라 출입 | 프레임 최신성 일치, 자원 해제, 실제 수집은 유지 |
| A18 | GNSS | 폰/장비 위치 및 RTCM/FIX 구분 |
| A19 | 큰 글꼴 | 360dp·OS 최대 글꼴에서 주요 정보/동작 접근 |
| A20 | 어두운 테마 | 모든 텍스트·상태·모달 대비와 가독성 |
| A21 | 인셋/키보드 | 마지막 행·확정 버튼 접근, 하단 이중 여백 없음 |
| A22 | TalkBack | 이름·역할·선택·비활성 이유·모달 focus 적절 |
| A23 | 샘플 데이터 격리 | release에서 예시 장비/가짜 정상/갤러리 노출 없음 |
| A24 | 기존 기능 연결표 | 등록·작업·저장소·서버·센서·RTK·팬·알림 누락 없음 |

클릭 가능한 행 안의 체크박스는 하나의 논리적 선택 동작으로 처리한다. 색만으로 상태를 알리지 않는다. 매초 변하는 시간을 TalkBack live region으로 계속 읽지 않는다. 중요한 오류를 한 줄 ellipsis로 숨기지 않는다.

### 10.4 실기기·성능

실제 휴대전화·Jetson의 ID와 OS, 연결 방식, 장비 서비스와 앱 빌드를 기록한다. 장비 등록/재접속, 승인된 작업 시작/중지, 결과 탐색, 작은 테스트 전송, 센서·영상·GNSS, 백그라운드 복귀를 대조한다.

운영 중인 장비에서 끊김 유도, 재부팅, 전원 종료, 네트워크 전환, 파일 삭제는 승인 없이 하지 않는다. 모의 테스트와 실제 장비 검증을 분리한다.

동일 조건으로 앱 시작, 스크롤, 메모리, APK 크기, 영상 출입 후 자원, 추가 network request/폴링을 전후 비교한다. 시각 업데이트가 불필요한 재연결이나 중복 구독을 만들면 롤백/수정한다. 측정하지 않은 개선율·FPS·배터리 절감률은 쓰지 않는다.

---

## 11. 최종 산출물과 보고 방식

구현 에이전트는 단순 분석에서 끝내지 말고 실제 코드·테스트·필요한 마이그레이션과 함께 다음을 남긴다.

`docs/UI_UX_DESIGN_SYSTEM.md`에는 확정된 tokens·타이포·컴포넌트·4탭 역할·상태 문구·실제 장치 때문에 조정한 디자인을 기록한다. 동등한 기존 파일이 있으면 갱신한다.

`docs/UI_UX_REBUILD_REPORT.md`에는 브랜치·HEAD·변경 단위, 기능 연결표, 자동 테스트 결과, 실제 장비별 검사, native 변경 전후 캡처, Figma 작업 상태, 차단 항목을 기록한다.

기능 상태는 `INTEGRATED_TESTED`, `PREVIEW_ONLY`, `UNSUPPORTED_ON_DEVICE`, `INTENTIONALLY_RELOCATED`, `BLOCKED` 등으로 구분한다. 코드 연결 확인과 실기 검증을 별도 열로 관리한다. 단순 미연결을 의도적 기능 제거라고 쓰지 않는다.

최종 판정은 **①네이티브 디자인 반영 ②기능 통합·자동 검증 ③실기기 검증 ④Figma 생성/검수/내보내기**를 각각 보고한다. 로컬 HTML 검사를 Android 통과로, Figma용 코드 작성을 Figma 파일 생성 완료로 바꾸지 않는다.

---

## 12. 참고 근거

아래는 V4에 수록된 2026-09-08 조사 출처를 이어받은 참고 자료다. 이번 V5 묶음은 제공 자료 정합성 작업이며 외부 정책·버전·요금을 새로 조사한 결과가 아니다. 외부 자료는 원칙과 API의 근거이고 Cobalt Field 팔레트·배치·추가 기능은 이 앱을 위한 새 설계다. 자료의 일반 권고가 이 제품의 사용자 테스트 결과를 대신하지 않는다.

- [D1] Google Design, **Inside M3 Expressive** — 목적 있는 표현력과 유연성: <https://design.google/library/design-notes-material-3-expressive-liam-spradlin>
- [D2] Apple Developer, **Meet Liquid Glass, WWDC25** — 콘텐츠와 제어/내비게이션 레이어: <https://developer.apple.com/videos/play/wwdc2025/219/>
- [D3] Android Developers, **Compose accessibility: API defaults** — 48dp와 semantics: <https://developer.android.com/develop/ui/compose/accessibility/api-defaults>
- [D4] Android Developers, **Android 14 features: nonlinear font scaling to 200%**: <https://developer.android.com/about/versions/14/features>
- [F1] Figma Developer Docs, **Plugin Manifest** — 실제 plugin ID, dynamic-page, networkAccess: <https://developers.figma.com/docs/plugins/manifest/>
- [F2] Figma Developer Docs, **Plugin Quickstart Guide**: <https://developers.figma.com/docs/plugins/plugin-quickstart-guide/>
- [F3] Figma Developer Docs, **exportAsync** — 실제 노드 export: <https://developers.figma.com/docs/plugins/api/properties/nodes-exportasync/>
- [R1] 이전 조사 baseline의 `UploadViewModel.kt`: <https://github.com/dbparkJ/JetsonControllerApp/blob/d9764e756ae64161acd12c9e7bf93ca18ea98909/app/src/main/java/com/example/jetsoncontroller/ui/upload/UploadViewModel.kt>
- [R2] 같은 baseline의 `TransportPolicy.kt`: <https://github.com/dbparkJ/JetsonControllerApp/blob/d9764e756ae64161acd12c9e7bf93ca18ea98909/app/src/main/java/com/example/jetsoncontroller/data/transport/TransportPolicy.kt>
- 사용자 근거: `reference/manifest.json`의 S01~S08 및 원본 캡처.

---

## 12.1 이 세트를 사용하는 시작 프롬프트

```text
00_START_HERE.md와 JETSONCONTROLLER_COBALT_FIELD_V5.md를 읽고,
이 폴더의 COBALT_FIELD_STYLE_GUIDE.md, design/tokens.json,
screens/의 화면 시안을 기준으로 dbparkJ/JetsonControllerApp을 리빌딩해.

이번 기준은 마지막에 보여준 Cobalt Field V5야.
네이비·코발트 블루·아이스 블루·오프화이트를 사용하고,
V4의 초록·청록·포리스트·라임 팔레트로 되돌리지 마.
이전 생성형 홍보 이미지는 구현 기준이 아니야.
홈·작업·데이터·설정 4탭과 작업 중심 UX, 기존 안전·복원 계약은 유지해.

먼저 이 지시 MD의 실제 경로, AGENTS.md, 현재 브랜치·HEAD·미커밋 변경,
기존 연결 안정성 코드를 확인하고 사용자 작업을 보존해.
이미 리빌딩된 부분은 재작성하지 말고 필요한 차이만 반영하며,
아직 구형 UI인 부분은 문서의 새 구조로 구현해.

문서는 기본안이지 실제 장치보다 우선하는 절대 명세가 아니야.
스마트폰·Jetson·API를 확인해 맞지 않는 부분은 스스로 근거를 검토해 조정하고,
무엇을 왜 바꿨는지 결과 보고서에 남겨.
실제 전송 단위, 연결 방식별 capability, RTK 중계와 파일 업로드의 차이,
요청 접수와 실행 확인, 센서·영상의 최신성을 사실대로 표현해.

PNG를 화면에 붙이거나 HTML을 제품 WebView로 넣지 말고
실제 Compose UI로 구현한 뒤 기존 Repository와 기능을 재연결해.
시안 수치와 정상 상태는 Preview/debug에서만 사용해.
기존 등록·인증·측정 파일·초안·네트워크 안정성·안전장치를 훼손하지 마.

Figma를 활용할 수 있으면 실제 파일 상태를 읽고 사용해.
접근이 막히면 figma-plugin의 코드와 PNG·토큰으로 가능한 구현을 계속하되,
로컬 시안과 실제 Figma 생성·내보내기 결과를 구분해 보고해.
Claude 링크 재접속이나 Figma 접근을 전체 작업의 선행 조건으로 삼지 마.

분석에서 끝내지 말고 구현 → 빌드·자동 테스트 → 접근 가능한 승인 실기기 검증까지 진행해.
밝은/어두운 테마, 큰 글꼴, 연결 복구, 장비 전환과 늦은 응답,
중복 명령 방지와 선택·초안 보존을 확인해.
운영 장비의 전원·네트워크·파일은 임의로 변경하지 말고,
실행하지 못한 검증을 통과했다고 보고하지 마.

최종 스타일 규칙, 변경 전후 실제 앱 캡처와 검증 결과를 별도로 남겨.
문서의 전체 완료 조건을 충족하면 마지막 단계에서
처음 기록한 JETSONCONTROLLER_COBALT_FIELD_V5.md 한 파일만 삭제해.
필수 구현이나 검증이 남아 있으면 이 MD를 유지하고 차단 사유를 명확히 보고해.
```

---

## 13. 마지막 정리 — 작업 완료 후 이 지시 MD만 제거

**작업 시작 때 기록한 `JETSONCONTROLLER_COBALT_FIELD_V5.md`의 정확한 경로 한 개만 삭제한다.** 삭제는 아래 조건을 모두 판정하고 결과를 남긴 뒤 마지막 단계에서 수행한다.

1. 합의된 네이티브 화면/기능이 구현됐고 의도적 이동·미지원·제외의 이유가 기록되어 있다.
2. 필수 자동 검사와 승인된 범위의 필수 실기기 검증이 완료됐다. 테스트/장치 접근이 막힌 상태를 통과로 쓰지 않았다.
3. 최종 스타일 가이드·결과 보고서·실제 앱 캡처가 별도 영구 위치에 남아 있다.
4. Figma 실행·검수·내보내기 상태를 사실대로 보고했다. 이번 구현 범위에서 필수인 Figma 작업이 남았다면 완료로 처리하지 않았다.
5. 사용자 데이터·등록/인증·초안·기존 연결 안정성과 안전장치가 보존됐다.

필수 구현·검증·인계가 남았으면 이 MD를 유지하고 차단 이유와 다음 행동을 남긴다. 새 작업을 가능하게 하려면 사용자 승인이나 접근이 필요하다는 사실을 명확히 적되 독립적인 작업은 계속한다. 명시적으로 범위가 변경된 경우에만 그 변경을 근거로 다시 완료 판정한다.

`COBALT_FIELD_STYLE_GUIDE.md`, `START_PROMPT.txt`, 원본 캡처, 디자인 파일, 코드·테스트, 다른 지시 문서, `AGENTS.md`는 이 정리 지시의 삭제 대상이 아니다. 와일드카드 삭제, 폴더 전체 삭제, MD 일괄 삭제를 금지한다.
