# AG03 Android 연결·복구

Branch: `work/android-connectivity-20260914`
Model: GPT-5.6 Sol XHigh

소유: Android transport/network/연결 repository와 관련 unit tests. `ui/**`는 수정하지 않는다.

여러 등록 장치 중 한 대 선택, 상황별 LAN/Wi-Fi Direct 기본 경로, 실패 시 대안, 화면 잠금/일시 단절, 재연결 후 Jetson 실제 상태 재조회, 시작/중지 응답 유실·늦은 응답·중복 명령을 검증한다. 앱 연결 단절을 수집 중단으로 추정하지 않는다.

작업 후 `AGENT_03_EXECUTION_REPORT.md`.
