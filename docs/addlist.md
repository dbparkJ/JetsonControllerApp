1. ~~이제 모든센서들이 연결되어있는 상태야 센서 활성화 되어 있는지 체크하는 로직 추가해줘~~
   - 처리: 카메라·GNSS·IMU의 실제 heartbeat를 1초마다 수집하고, 마지막 샘플이 3초 이상 갱신되지 않으면 비활성으로 판정하도록 추가했습니다. 서비스 실행 여부와 실제 센서 연결/데이터 수신 상태를 분리해 센서 탭에 표시합니다.

2. ~~GNSS의 경우 상태가 rtk가 연결되어 있어서 gps, dgps, rtk fix, rtk float등으로 상태가 되어 있거든 이 상태를 작업탭에도 실시간으로 확인가능하게 추가해주고 센서 상태에서도 볼 수 있게 해줘~~
   - 처리: NMEA fix quality를 GPS, DGPS, PPS, RTK Fix, RTK Float 등으로 변환하고 NTRIP 연결·기준국·위성 수·HDOP와 함께 센서 탭 및 작업 탭에서 1초 주기로 확인할 수 있게 했습니다.

3. ~~카메라 센서의 경우 센서탭에서 눌러서 실시간으로 카메라데이터를 앱으로 스트리밍할 수 있게 해줘 이경우에는 지금 레포 말고 /home/jm/26_camera_record/depthai_refactored_ver2 여기에 추가해주자~~
   - 처리: 카메라 저장소에 비동기 프리뷰 브리지를 추가해 최신 RGB를 최대 4 FPS, 1280 px JPEG로 게시합니다. Jetson API의 인증·응답 서명을 거쳐 앱 카메라 화면에서 250 ms 간격으로 갱신하며 실기 영상 수신까지 확인했습니다.

4. ~~GPS데이터가 들어오면 센서에 GPS로 터치하면 VWorld 2D, 위성지도 API를 연동해서 앱 위에 초당 한번의 데이터를 갱신할 수 있게 해줘~~
   - 처리: 센서 및 작업 탭의 GNSS 항목에서 VWorld 일반지도/위성지도 화면으로 이동하도록 추가했습니다. MapLibre raster 지도, 현재 위치 마커, 좌표·fix·NTRIP 정보를 1초마다 갱신하며 두 지도 타일의 실기 표시를 확인했습니다. 개발 키는 추적 파일에서 제거하고 로컬 빌드 설정으로만 주입했습니다.

5. /home/jm/26_camera_record 레포를 지금 앱이랑 연동하기 좋게 추가해야할 부분 있으면 추가해줘 그리고 저쪽 레포도 커밋해주고 푸쉬까지 진행해줘
   - 처리 현황: 센서 상태 JSON·카메라 프리뷰 브리지, 설정값, 테스트, 연동 문서를 추가했고 전체 21개 테스트 통과 후 `c50502a`로 커밋했습니다. `/home/jm/26_camera_record/.git`의 설정·브랜치·reflog와 셸 기록까지 확인했지만 등록됐거나 과거에 사용한 원격 저장소 주소가 없어 push만 보류했습니다. 원격 저장소 주소 또는 새 저장소 이름과 공개 범위가 필요합니다.

6. 모든 작업마치면 이쪽레포도 커밋 푸쉬해주고 앱 빌드 다시해야하면 빌드하고 모바일에도 빌드까지 진행해줘
   - 처리 현황: 백엔드 1.7.0과 깨끗한 카메라 스냅샷을 Jetson에 배포했고, Android 1.10.0 APK를 빌드해 Galaxy S22 Ultra에 설치했습니다. 앱 구현은 `3105fd2`로 커밋해 `origin/main`에 push했습니다. 5번의 카메라 원격 push가 완료되면 이 항목도 최종 완료 처리합니다.
